export interface IThemeColor {
  background: { default: string };
  onBackground: {
    default: string;
    opacity0_08: string;
  };
  primary: {
    default: string;
  };
  onPrimary: {
    default: string;
    opacity0_12: string;
  };
}
